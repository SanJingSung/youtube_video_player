import React from 'react';
import { Button } from 'react-bootstrap';

function BackButton() {
    return (
        <Button >
            ← Back
        </Button>
    );
}

export default BackButton;
